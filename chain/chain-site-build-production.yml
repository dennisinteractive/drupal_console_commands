# Usage: drupal site:build:prod --name="subscriptions"
command:
  name: site:build:prod
  description: 'Runs updates on Production.'
commands:
# Run updates.
   - command: site:update
     arguments:
       name: '%{{name}}'
